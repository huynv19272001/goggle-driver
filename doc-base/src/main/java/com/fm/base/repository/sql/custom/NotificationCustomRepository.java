package com.fm.base.repository.sql.custom;

import com.fm.base.models.sql.Notification;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

public interface NotificationCustomRepository {
//    Page<Notification> searchByFileFolderName(Integer userId, String name, Pageable pageable);
//
//    Page<Notification> searchByParentIdAndUserId(Integer userId,Integer parentId, Pageable pageable);

}
